import React from 'react';
import ReactDOM from 'react-dom';
import './styles/Flowchart.css'; // Custom CSS for the app
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
